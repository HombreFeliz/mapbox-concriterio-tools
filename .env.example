# Token de servidor — usado en API routes (geocoding proxy)
MAPBOX_ACCESS_TOKEN=pk.your_server_token_here

# Token público — usado en el cliente para inicializar el mapa
# Puede ser el mismo token si se configuran URL restrictions en Mapbox Dashboard
NEXT_PUBLIC_MAPBOX_TOKEN=pk.your_public_token_here
